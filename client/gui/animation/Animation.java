package client.gui.animation;

import javafx.scene.Node;

import java.util.HashSet;

public abstract class Animation {
    protected final static HashSet<Node> protectedAnimations = new HashSet<>();
}